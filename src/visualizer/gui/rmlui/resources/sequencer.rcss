body {
    font-family: Inter;
    display: flex;
    flex-direction: column;
    margin: 0;
    padding: 0;
    overflow: hidden;
    width: 100%;
    height: 100%;
}

#panel {
    display: flex;
    flex-direction: column;
    width: 100%;
    height: 100%;
    min-height: 0;
    padding: 3dp 16dp 0 16dp;
    box-sizing: border-box;
    position: relative;
    overflow: visible;
}

#panel.is-floating {
    padding-top: 10dp;
    padding-bottom: 6dp;
}

#floating-header {
    min-height: 28dp;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8dp;
    padding: 0 4dp;
    margin-bottom: 6dp;
}

#floating-title {
    font-size: 12dp;
    font-weight: bold;
    color: #cdd6f4;
}

#floating-actions {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 6dp;
    flex-shrink: 0;
}

.floating-header-action {
    min-width: 64dp;
}

#transport-row {
    display: flex;
    flex-direction: row;
    width: 100%;
    height: 36dp;
    margin-bottom: 3dp;
    border-bottom: 1dp rgba(160, 160, 180, 40);
    align-items: center;
    justify-content: space-between;
}

#left-controls {
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 4dp;
    flex-shrink: 0;
}

#right-controls {
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 4dp;
    flex-shrink: 0;
}

#transport {
    display: flex;
    flex-direction: row;
    align-items: center;
}

.transport-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 26dp;
    height: 26dp;
    border-radius: 4dp;
    flex-shrink: 0;
    transition: background-color 0.12s quadratic-out;
    position: relative;
}

.transport-btn:hover {
    background-color: rgba(255, 255, 255, 30);
}

.transport-btn:active {
    background-color: rgba(255, 255, 255, 50);
}

.transport-btn.active {
    background-color: rgba(100, 160, 255, 80);
}

.transport-btn.toggle {
    border-radius: 4dp;
}

.transport-btn.text {
    width: auto;
    padding: 0 8dp;
    min-width: 26dp;
}

.transport-btn.text.wide {
    min-width: 40dp;
}

.transport-btn.primary {
    background-color: rgba(100, 160, 255, 40);
}

.transport-btn.primary:hover {
    background-color: rgba(100, 160, 255, 70);
}

.transport-btn.error:hover {
    background-color: rgba(255, 100, 100, 40);
}

.transport-btn.disabled {
    opacity: 0.35;
    pointer-events: none;
}

.ctx-indicator {
    position: absolute;
    bottom: -1dp;
    right: 0;
    font-size: 12dp;
    line-height: 12dp;
    color: rgba(200, 210, 230, 140);
}

.transport-icon {
    width: 20dp;
    height: 20dp;
    image-color: #cdd6f4;
}

.transport-icon.small {
    width: 16dp;
    height: 16dp;
}

.transport-label {
    font-size: 12dp;
    white-space: nowrap;
}

.transport-label-short {
    display: none;
}

.transport-info {
    font-size: 12dp;
    opacity: 0.6;
    flex-shrink: 0;
}

.transport-info.quality-group {
    margin-left: 2dp;
}

#resolution-field {
    position: relative;
    display: flex;
    align-items: center;
    padding: 0 4dp;
    min-height: 18dp;
    border-width: 1dp;
    border-color: transparent;
    border-radius: 4dp;
    cursor: pointer;
    transition: border-color 0.12s quadratic-out, background-color 0.12s quadratic-out;
}

#resolution-field:hover,
#resolution-field.is-editing {
    border-color: #45475a;
    background-color: #313244;
}

#resolution-field.is-editing {
    cursor: text;
    opacity: 1;
}

#resolution-info,
#resolution-input {
    font-family: "JetBrains Mono";
    font-size: 12dp;
    color: #cdd6f4;
}

#resolution-info {
    white-space: nowrap;
}

#resolution-input {
    display: none;
    width: 80dp;
    padding: 0;
    margin: 0;
    min-height: 0;
    background-color: transparent;
    border-width: 0;
    box-shadow: none;
}

#resolution-field.is-editing #resolution-info {
    display: none;
}

#resolution-field.is-editing #resolution-input {
    display: block;
}

#resolution-input:focus,
#resolution-input:focus-visible {
    border-width: 0;
    box-shadow: none;
}

.transport-sep {
    width: 1dp;
    height: 20dp;
    flex-shrink: 0;
    background-color: rgba(160, 160, 180, 60);
}

/* ── Quality Scrub Field ───────────────────────────────── */

#quality-scrub {
    position: relative;
    overflow: hidden;
    width: 60dp;
    min-width: 50dp;
    min-height: 18dp;
    padding: 0 6dp;
    display: flex;
    align-items: center;
    justify-content: flex-end;
    box-sizing: border-box;
    border-width: 1dp;
    border-radius: 6dp;
    font-family: "JetBrains Mono";
    font-size: 12dp;
    cursor: ew-resize;
    transition: border-color 0.12s quadratic-out, background-color 0.12s quadratic-out;
}

#quality-scrub.is-editing {
    cursor: text;
}

#quality-fill {
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 0%;
    border-radius: 5dp;
}

#quality-display,
#quality-input {
    position: relative;
    z-index: 1;
    width: 100%;
    text-align: right;
    font-family: "JetBrains Mono";
    font-size: 12dp;
}

#quality-display {
    white-space: nowrap;
    overflow: hidden;
}

#quality-input {
    display: none;
    padding: 0;
    margin: 0;
    min-height: 0;
    background-color: transparent;
    border-width: 0;
    box-shadow: none;
}

#quality-scrub.is-editing #quality-display {
    display: none;
}

#quality-scrub.is-editing #quality-input {
    display: block;
}

#quality-input:focus,
#quality-input:focus-visible {
    border-width: 0;
    box-shadow: none;
}

#btn-equirect {
    min-width: 46dp;
}

/* --- Transport overrides --- */

.cam-path-icon {
    width: 14dp;
    height: 14dp;
    margin-right: 2dp;
}

.play-btn {
    width: 30dp;
    height: 30dp;
    border-radius: 15dp;
}

.speed-val {
    font-size: 12dp;
    font-weight: bold;
}

.speed-text {
    font-size: 12dp;
    opacity: 0.5;
    margin-left: 3dp;
}

.dropdown-arrow {
    font-size: 12dp;
    opacity: 0.4;
    margin-left: 2dp;
}

.sequence-fps-field {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: flex-end;
    width: 58dp;
    min-height: 22dp;
    padding: 0 6dp;
    box-sizing: border-box;
    border-width: 1dp;
    border-color: transparent;
    border-radius: 4dp;
    cursor: pointer;
    transition: border-color 0.12s quadratic-out, background-color 0.12s quadratic-out;
}

.sequence-fps-field:hover,
.sequence-fps-field.active,
.sequence-fps-field.is-editing {
    border-color: #45475a;
    background-color: #313244;
}

.sequence-fps-field.is-editing {
    cursor: text;
}

#sequence-fps-display,
#sequence-fps-input {
    width: 100%;
    text-align: right;
    font-family: "JetBrains Mono";
    font-size: 12dp;
    color: #cdd6f4;
}

#sequence-fps-display {
    white-space: nowrap;
    overflow: hidden;
}

#sequence-fps-input {
    display: none;
    padding: 0;
    margin: 0;
    min-height: 0;
    background-color: transparent;
    border-width: 0;
    box-shadow: none;
}

.sequence-fps-field.is-editing #sequence-fps-display {
    display: none;
}

.sequence-fps-field.is-editing #sequence-fps-input {
    display: block;
}

#sequence-fps-input:focus,
#sequence-fps-input:focus-visible {
    border-width: 0;
    box-shadow: none;
}

.snap-check {
    width: 10dp;
    height: 10dp;
    border-width: 1dp;
    border-radius: 2dp;
    margin-right: 3dp;
}

.format-badge {
    min-width: 60dp;
    padding: 0 7dp;
}

.export-icon {
    width: 13dp;
    height: 13dp;
    margin-right: 3dp;
}

.clear-icon {
    width: 13dp;
    height: 13dp;
    margin-right: 3dp;
}

#header {
    display: flex;
    width: 100%;
    min-height: 56dp;
    flex: 1 1 56dp;
    min-width: 0;
}

#timeline {
    display: flex;
    flex-direction: column;
    width: 100%;
    height: 100%;
    min-height: 0;
    position: relative;
    overflow: hidden;
}

#ruler {
    display: block;
    width: 100%;
    height: 16dp;
    position: relative;
    overflow: visible;
}

.ruler-tick {
    display: block;
    position: absolute;
    top: 0;
    width: 1dp;
    background-color: rgba(160, 160, 180, 120);
}

.ruler-tick.major {
    height: 8dp;
    background-color: rgba(160, 160, 180, 180);
}

.ruler-tick.minor {
    height: 4dp;
    top: 4dp;
    background-color: rgba(160, 160, 180, 80);
}

.ruler-label {
    display: inline-block;
    position: absolute;
    top: 0;
    font-size: 12dp;
    color: rgba(200, 210, 230, 220);
    white-space: nowrap;
    transform: translateX(-50%);
}

#track-area {
    display: block;
    width: 100%;
    flex: 1;
    min-height: 28dp;
    position: relative;
    margin-top: 4dp;
}

#track-bar {
    display: block;
    position: absolute;
    left: 0;
    top: 50%;
    width: 100%;
    height: 24dp;
    margin-top: -12dp;
    border-radius: 4dp;
}

#hint {
    display: block;
    position: absolute;
    top: 50%;
    left: 0;
    width: 100%;
    margin-top: -6dp;
    text-align: center;
    font-size: 12dp;
}

#sequence-strip {
    display: block;
    position: absolute;
    left: 0;
    top: 50%;
    width: 100%;
    height: 22dp;
    margin-top: -11dp;
    overflow: hidden;
    pointer-events: none;
}

.sequence-clip {
    display: block;
    position: absolute;
    top: 2dp;
    height: 18dp;
    min-width: 2dp;
    box-sizing: border-box;
    border-width: 1dp;
    border-color: rgba(105, 175, 255, 190);
    border-radius: 4dp;
    background-color: rgba(70, 120, 190, 95);
}

.sequence-clip-label {
    display: block;
    position: absolute;
    left: 8dp;
    right: 8dp;
    top: 2dp;
    height: 14dp;
    font-size: 11dp;
    line-height: 14dp;
    color: rgba(230, 238, 255, 230);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

.sequence-frame-tick {
    display: block;
    position: absolute;
    top: 0;
    bottom: 0;
    width: 1dp;
    background-color: rgba(230, 238, 255, 95);
}

#keyframes {
    display: block;
    position: absolute;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
}

.keyframe {
    display: block;
    position: absolute;
    width: 14dp;
    height: 14dp;
    top: 50%;
    margin-top: -7dp;
    margin-left: -7dp;
    transform: rotate(45deg);
    border-radius: 1dp;
    pointer-events: none;
}

.keyframe.selected {
    border-width: 1dp;
}

.keyframe.loop-point {
    transform: none;
    border-radius: 6dp;
    width: 10dp;
    height: 10dp;
    top: 50%;
    margin-top: -5dp;
    margin-left: -5dp;
}

#playhead {
    display: block;
    position: absolute;
    top: 0;
    width: 0;
    height: 100%;
}

#playhead-handle {
    display: block;
    position: absolute;
    top: -14dp;
    left: -7dp;
    width: 14dp;
    height: 14dp;
    background-color: #ff6688;
    border-radius: 7dp;
    pointer-events: none;
    transform-origin: 50% 50%;
    transition: transform 0.12s quadratic-out, background-color 0.12s quadratic-out;
}

#playhead-handle.hot {
    transform: scale(1.2);
    background-color: #ff99aa;
}

#timeline-scrollbar {
    display: none;
    position: absolute;
    left: 0;
    bottom: 0;
    width: 100%;
    height: 6dp;
    border-radius: 3dp;
    background-color: rgba(160, 160, 180, 40);
    overflow: hidden;
}

#timeline-scrollbar.visible {
    display: block;
}

#timeline-scrollbar-thumb {
    display: block;
    position: absolute;
    top: 0;
    height: 100%;
    border-radius: 3dp;
    background-color: rgba(200, 210, 230, 120);
}

#timeline-scrollbar.hot #timeline-scrollbar-thumb {
    background-color: rgba(230, 238, 255, 200);
}

#time-display {
    display: flex;
    flex-direction: row;
    align-items: center;
    margin-left: 8dp;
    padding-left: 8dp;
}

#current-time {
    font-size: 12dp;
}

#duration-field {
    position: relative;
    display: flex;
    align-items: center;
    margin-left: 2dp;
    padding: 0 4dp;
    min-height: 18dp;
    border-width: 1dp;
    border-color: transparent;
    border-radius: 4dp;
    cursor: pointer;
    transition: border-color 0.12s quadratic-out, background-color 0.12s quadratic-out;
}

#duration-field:hover,
#duration-field.is-editing {
    border-color: #45475a;
    background-color: #313244;
}

#duration-field.is-editing {
    cursor: text;
}

#duration,
#duration-input {
    font-family: "JetBrains Mono";
    font-size: 12dp;
    color: #cdd6f4;
}

#duration {
    white-space: nowrap;
}

#duration-input {
    display: none;
    width: 60dp;
    padding: 0;
    margin: 0;
    min-height: 0;
    background-color: transparent;
    border-width: 0;
    box-shadow: none;
}

#duration-field.is-editing #duration {
    display: none;
}

#duration-field.is-editing #duration-input {
    display: block;
}

#duration-input:focus,
#duration-input:focus-visible {
    border-width: 0;
    box-shadow: none;
}

#easing-stripe {
    display: block;
    width: auto;
    height: 36dp;
    box-sizing: border-box;
    border-top: 1dp rgba(160, 160, 180, 40);
    position: relative;
    overflow: hidden;
    margin-left: -16dp;
    margin-right: -16dp;
}

#easing-segments,
#easing-curves,
#easing-indicators {
    display: block;
    position: absolute;
    left: 16dp;
    right: 16dp;
    top: 0;
    height: 100%;
    pointer-events: none;
}

.easing-segment {
    display: block;
    position: absolute;
    top: 0;
    height: 100%;
}

.easing-curve-segment {
    display: block;
    position: absolute;
    height: 1.5dp;
    border-radius: 1dp;
    transform-origin: 0 50%;
}

.easing-dot {
    display: block;
    position: absolute;
    width: 6dp;
    height: 6dp;
    margin-left: -3dp;
    margin-top: -3dp;
    border-radius: 3dp;
}

.easing-indicator {
    display: block;
    position: absolute;
}

.easing-indicator.ease-in,
.easing-indicator.ease-out {
    width: 0;
    height: 0;
    margin-left: -4dp;
    border-left-width: 4dp;
    border-left-color: rgba(0, 0, 0, 0);
    border-right-width: 4dp;
    border-right-color: rgba(0, 0, 0, 0);
}

.easing-indicator.ease-in {
    border-bottom-width: 4dp;
}

.easing-indicator.ease-out {
    border-top-width: 4dp;
}

.easing-indicator.ease-in-out {
    width: 8dp;
    height: 8dp;
    margin-left: -4dp;
    border-radius: 2dp;
    transform: rotate(45deg);
}

#film-strip-panel {
    display: block;
    width: auto;
    height: 56dp;
    position: relative;
    margin-top: -1px;
    margin-left: -16dp;
    margin-right: -16dp;
    overflow: hidden;
}

#film-strip-groove {
    display: block;
    position: absolute;
    left: 12px;
    right: 12px;
    top: 4px;
    bottom: 4px;
    border-radius: 4px;
    overflow: hidden;
}

#film-strip-gaps,
#film-strip-thumbs,
#film-strip-dividers,
#film-strip-markers,
#film-strip-sprockets-top,
#film-strip-sprockets-bottom {
    display: block;
    position: absolute;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
}

.film-strip-gap {
    display: block;
    position: absolute;
    top: 0;
    bottom: 0;
}

.film-strip-gap-stripe {
    display: block;
    position: absolute;
    width: 1dp;
    transform-origin: 0 0;
}

.film-thumb {
    display: block;
    position: absolute;
    top: 0;
    bottom: 0;
    border-radius: 3px;
    overflow: hidden;
}

.film-thumb-image {
    display: block;
    width: 100%;
    height: 100%;
    image-color: #ffffff;
}

.film-thumb-tint,
.film-thumb-edge,
.film-thumb-outline,
.film-thumb-midline {
    display: block;
    position: absolute;
    pointer-events: none;
}

.film-thumb-tint {
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
}

.film-thumb-edge.top,
.film-thumb-edge.bottom {
    left: 0;
    width: 100%;
    height: 3px;
    opacity: 0;
}

.film-thumb-edge.top {
    top: 0;
}

.film-thumb-edge.bottom {
    bottom: 0;
}

.film-thumb-outline {
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    border-radius: 3px;
    border-width: 0;
    border-color: transparent;
}

.film-thumb-midline {
    top: 4px;
    bottom: 4px;
    left: 50%;
    width: 1px;
    margin-left: -0.5px;
}

.film-thumb-midline.shadow {
    width: 3px;
    margin-left: -1.5px;
}

.film-thumb.stale .film-thumb-image {
    opacity: 0.58;
}

.film-thumb.contains-hovered-keyframe .film-thumb-tint.hovered-keyframe,
.film-thumb.contains-selected .film-thumb-tint.selected,
.film-thumb.contains-hovered-keyframe .film-thumb-edge,
.film-thumb.contains-selected .film-thumb-edge {
    opacity: 1;
}

.film-thumb.hovered .film-thumb-outline,
.film-thumb.contains-hovered-keyframe .film-thumb-outline,
.film-thumb.contains-selected .film-thumb-outline {
    border-width: 1px;
}

.film-thumb.contains-selected .film-thumb-outline {
    border-width: 2px;
}

.film-strip-divider {
    display: block;
    position: absolute;
    top: 4px;
    bottom: 4px;
    width: 1px;
}

.film-strip-marker {
    display: block;
    position: absolute;
    top: 0;
    bottom: 0;
    width: 0;
}

.film-strip-marker-line {
    display: block;
    position: absolute;
    top: 4px;
    bottom: 4px;
    left: 0;
}

.film-strip-marker-line.shadow {
    width: 3px;
    margin-left: -1.5px;
}

.film-strip-marker-line.main {
    width: 1.5px;
    margin-left: -0.75px;
}

.film-strip-marker-cap {
    display: block;
    position: absolute;
    left: -2px;
    width: 4px;
    height: 4px;
    border-radius: 1.5px;
}

.film-strip-marker-cap.top {
    top: 4px;
}

.film-strip-marker-cap.bottom {
    bottom: 4px;
}

.film-strip-sprocket {
    display: block;
    position: absolute;
    width: 4px;
    height: 3px;
    border-radius: 1px;
}

.film-strip-sprocket.top {
    top: 0.5px;
}

.film-strip-sprocket.bottom {
    bottom: 0.5px;
}

#panel-guides {
    display: block;
    position: absolute;
    pointer-events: none;
}

.timeline-guide {
    display: none;
    position: absolute;
    top: 0;
    bottom: 0;
}

#timeline-tooltip {
    display: none;
    position: absolute;
    min-width: 112dp;
    max-width: 220dp;
    padding: 7dp 10dp;
    border-width: 1dp;
    border-radius: 8dp;
    pointer-events: none;
    box-sizing: border-box;
}

.timeline-tooltip-line {
    display: block;
    white-space: nowrap;
    font-size: 12dp;
}

.timeline-tooltip-line.title {
    font-size: 12dp;
    margin-bottom: 2dp;
}

@media (max-width: 1240dp) {
    .ctx-indicator {
        display: none;
    }

    #btn-camera-path .transport-label,
    #btn-export .transport-label,
    #btn-clear .transport-label {
        display: none;
    }

    #btn-camera-path .cam-path-icon,
    #btn-export .export-icon,
    #btn-clear .clear-icon {
        margin-right: 0;
    }
}

@media (max-width: 1120dp) {
    #btn-camera-path,
    #btn-snap,
    #btn-follow,
    #btn-preview,
    #btn-format,
    #btn-clear,
    .transport-sep {
        display: none;
    }
}

@media (max-width: 1040dp) {
    #btn-film-strip,
    #btn-equirect,
    #btn-load-sequence {
        display: none;
    }
}

@media (max-width: 660dp) {
    .transport-label-full {
        display: none;
    }

    .transport-label-short {
        display: block;
    }
}

@media (max-width: 580dp) {
    #duration-field {
        display: none;
    }

    #time-display {
        margin-left: 0;
        padding-left: 4dp;
    }

    #left-controls,
    #right-controls {
        gap: 2dp;
    }

    #transport-row {
        justify-content: flex-start;
        gap: 6dp;
    }
}
